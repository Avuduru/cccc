export const config = {
    /**
     * The URL of the proxy server. 
     * Since the proxy is on the same server, we use a relative path.
     */
    WORKER_URL: 'proxy.php',

    // Design settings (Use cqw/cqh for canvas elements)
    DESIGN: {
        horizontal: {
            titleSize: '10cqw',
            genreSize: '5cqw',
            synopsisSize: '4cqw'
        },
        vertical: {
            titleSize: '10cqw'
        }
    }
};

